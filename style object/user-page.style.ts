import styled from "@emotion/styled";
import { IconButton } from "@mui/material";

export const SearchIconButtonStyle = styled(IconButton)({
  backgroundColor: '#1565c0',
  width: 36,
  height: 36,
});